---
import SiteHeader from './islands/SiteHeader.tsx';
const { user } = Astro.locals;
const currentPath = Astro.url.pathname;
---
<SiteHeader client:load user={user ? { id: user.id, full_name: user.full_name, role: user.role } : null} currentPath={currentPath} />
