{
  "name": "@bocado/mobile-app",
  "version": "0.1.0",
  "private": true,
  "description": "App Android unificada BocadO (admin + repartidor)",
  "scripts": {
    "config:local": "node scripts/write-config.mjs local",
    "config:prod": "node scripts/write-config.mjs prod",
    "icons": "node scripts/generate-icons.mjs",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:apk": "cd android && ./gradlew assembleDebug",
    "build:apk:release": "cd android && ./gradlew assembleRelease"
  },
  "dependencies": {
    "@capacitor/android": "^8.0.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.0.0",
    "@capacitor/geolocation": "^8.0.0",
    "@capacitor/local-notifications": "^8.0.0",
    "@capacitor/splash-screen": "^8.0.0",
    "@capacitor/status-bar": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0",
    "sharp": "^0.35.3",
    "typescript": "^5.5.4"
  },
  "capacitor": {
    "platforms": [
      "android"
    ]
  }
}