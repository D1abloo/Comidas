services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: bocado
      POSTGRES_PASSWORD: bocado
      POSTGRES_DB: bocado
    volumes:
      - bocado_pg:/var/lib/postgresql/data
      - ./docker/postgres/init.sql:/docker-entrypoint-initdb.d/01-init.sql:ro
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U bocado -d bocado"]
      interval: 5s
      timeout: 5s
      retries: 10

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 4321
      DATABASE_URL: postgres://bocado:bocado@postgres:5432/bocado
      SESSION_SECRET: ${SESSION_SECRET:-cambia-este-secreto-en-produccion}
      PUBLIC_APP_URL: ${PUBLIC_APP_URL:-http://localhost:4321}
      BIZUM_COMPANY_PHONE: ${BIZUM_COMPANY_PHONE:-+34600123456}
    ports:
      - "4321:4321"

volumes:
  bocado_pg:
